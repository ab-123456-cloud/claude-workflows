# Add a multiply() function

**Type:** Tracer bullet

Add `multiply(a, b)` to `src/calculator.js`, exported like the existing functions.

## Acceptance criteria

- `multiply(4, 5)` returns `20`
- A test in `src/calculator.test.js` covers `multiply`
- `npm run test` passes
